# homebrew-agentctl

Homebrew tap for [agentctl](https://github.com/ylcn91/agentctl) — multi-account AI agent manager.

## Install

```bash
brew install ylcn91/agentctl/agentctl
```

Or:

```bash
brew tap ylcn91/agentctl
brew install agentctl
```

## Update

```bash
brew upgrade agentctl
```
